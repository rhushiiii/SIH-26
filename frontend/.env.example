# Set to "false" to connect to the live FastAPI backend, or "true" for mock data
VITE_USE_MOCK_API=true

# Base URL for the FastAPI backend API (version 1)
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Auth enabled toggle (true by default)
VITE_AUTH_ENABLED=true
